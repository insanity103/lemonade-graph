# legacy

Unmapped scripts kept for their reusable logic. Nothing here is synced by any Rojo project.

- `BossRoomGate.server.luau` — level-gated doors via collision groups (for future region gates).
- `HubLeaderboard.server.luau` — hub pillar listing top players (candidate for a Wealth board).
